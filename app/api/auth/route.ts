import { NextResponse } from "next/server";

// Placeholder route to satisfy Next.js route-module type-checking.
// If/when you implement NextAuth/Supabase auth, replace this file with real handlers.
export async function GET() {
  return NextResponse.json({ ok: true });
}

